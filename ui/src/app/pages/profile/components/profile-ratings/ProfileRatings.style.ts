import styled from 'styled-components';

export const ProfileRatingsWrapper = styled.div`

  .rating-items-list {
    display: flex;
    flex-wrap: wrap;
    width: fit-content;
    margin: 24px auto;
    flex-direction: column;
  }
`;
